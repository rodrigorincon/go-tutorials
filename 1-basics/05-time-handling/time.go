package main

import (
    "fmt"
    "strings"
)

func main() {
    // read Date and Time from computer

    // Date and Time formats

    // string format

    // check if a time in a range

    // check if a time is the last 2 hours

    // add minutes in a time

    // check if a time is in the future

    // check if a time is greater than other

    // get objects created in a time range
}